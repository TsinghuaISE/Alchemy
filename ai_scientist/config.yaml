model: "gemini-3-flash-preview"
base_url: "https://ssvip.dmxapi.com/v1"

tasks:
  - domain: Recsys
    task: MMRec
    metric: "recall@20"
    seeds: ["PGL_2025"]

  # - domain: timeseries
  #   task: long_term_forecast
  #   metric: "mse"
  #   seeds: ["iTransformer_2023"]

max_rounds: 10
patience: 3

# 并发配置
temperatures: [0.3, 0.5, 0.7, 0.9]   # 多样性采样
max_concurrent: 32                     # 最大并发实验数
