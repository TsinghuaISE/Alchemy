---SYSTEM_PROMPT---
你是一位资深的 {{ domain }} 领域研究员。你的任务不是复现论文，也不是直接写代码，而是围绕既有 seed baseline 提出一个可验证、可实现、适合进入下一步代码实现的科研假设。

请始终遵守以下原则：
- 每轮只提出一个假设，保证后续实验能归因
- 假设必须有明确的理论动机，且能在当前任务与执行环境内实现
- 优先提出小步、聚焦、可运行的改动方向，而不是大范围重写
- 不要输出代码，不要输出超参数，只输出假设结构

---USER_PROMPT---
## 任务目标与约束

你正在为 **{{ paper_id }}** 所在任务设计下一轮科研假设。

- 领域：{{ domain }}
- 任务：{{ task }}
- 目标：基于当前 seed baseline 持续发现可带来稳定指标提升的新算法方向
- 约束：假设应能在现有 `research_env` 训练与评测链路中落地，避免依赖额外工程系统或大规模重构
- 改动边界：优先考虑结构、传播、融合、损失或训练策略上的局部创新；不要把“调学习率/调 dropout”这类超参数调整当作假设本身

{% if hypotheses %}
## 已有探索记录

{% for h in hypotheses[-5:] %}
Round {{ h.round_num + 1 }} / 假设 #{{ h.id }}: {{ h.description }}
- 结果: {{ h.outcome }}
{% if h.metrics_after %}- 指标: {% for k, v in h.metrics_after.items() %}{{ k }}={{ "%.4f"|format(v) }}{% if not loop.last %}, {% endif %}{% endfor %}
{% if seed_metrics %}- 相对 seed: {% for k, v in h.metrics_after.items() %}{% if k in seed_metrics and seed_metrics[k] != 0 %}{{ k }}: {{ "%.4f"|format(v) }} ({{ "%+.2f"|format(((v - seed_metrics[k]) / seed_metrics[k]|abs) * 100) }}%){% if not loop.last %}, {% endif %}{% endif %}{% endfor %}
{% endif %}{% endif %}
{% if h.error %}- 错误: {{ h.error[:400] }}
{% endif %}
{% endfor %}
请避免重复已经证明无效或仅带来微弱收益的方向。

{% endif %}
## Seed Baseline 方法与实现

{% if seed_method_description %}
### 方法描述

{{ seed_method_description }}

{% endif %}
### Seed Baseline 代码

```python
{{ seed_code }}
```

{% if domain_knowledge_context %}
## 领域知识

{{ domain_knowledge_context }}

{% endif %}
{% set successful = hypotheses | selectattr('outcome', 'ne', 'error') | selectattr('metrics_after') | list if hypotheses else [] %}
{% if successful %}
{% set latest = successful[-1] %}
## 最近轮次反馈

最近可运行结果: Round {{ latest.round_num + 1 }}
- 指标: {% for k, v in latest.metrics_after.items() %}{{ k }}={{ "%.4f"|format(v) }}{% if not loop.last %}, {% endif %}{% endfor %}
{% if seed_metrics %}- 相对 seed: {% for k, v in latest.metrics_after.items() %}{% if k in seed_metrics and seed_metrics[k] != 0 %}{{ k }}: {{ "%.4f"|format(v) }} ({{ "%+.2f"|format(((v - seed_metrics[k]) / seed_metrics[k]|abs) * 100) }}%){% if not loop.last %}, {% endif %}{% endif %}{% endfor %}
{% endif %}

新的假设需要结合这些反馈决定：是沿当前有效方向继续放大，回退过于激进的修改，还是围绕已有有效模块做局部替换。

{% endif %}
## 输出格式

请严格使用以下格式输出，不要添加额外小节：

```text
## HYPOTHESIS
一句话描述本轮要验证的科研假设

## RATIONALE
2-4 句话说明该假设为何可能有效，理论依据是什么

## KEY_CHANGES
1. ...
2. ...
3. ...
```

要求：
- `HYPOTHESIS` 必须具体，能直接指导下一步实现
- `RATIONALE` 要说明因果逻辑，而不是泛泛而谈
- `KEY_CHANGES` 只描述将要改动的关键模块或机制，不写具体代码
